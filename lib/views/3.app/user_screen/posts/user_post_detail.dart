import 'package:flutter/material.dart';

class UserPostDetail extends StatelessWidget {
  static const routeName = '/userpostdetail';
  const UserPostDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
