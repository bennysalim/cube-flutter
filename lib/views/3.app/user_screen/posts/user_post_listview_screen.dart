import 'package:flutter/material.dart';

class UserPostListViewScreen extends StatelessWidget {
  const UserPostListViewScreen({super.key});
  static const routeName = '/userpostlistview';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
