import 'package:flutter/material.dart';

class MyFollowingScreen extends StatelessWidget {
  const MyFollowingScreen({super.key});
  static const routeName = '/myfollowing';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
